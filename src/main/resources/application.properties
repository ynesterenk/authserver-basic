# Default application properties for AWS Lambda
spring.profiles.active=aws

# Logging configuration
logging.level.root=INFO
logging.level.com.example.auth=DEBUG
logging.level.org.springframework=INFO

# Cache configuration
auth.cache-ttl-minutes=5

# AWS configuration - gets overridden by environment variables
auth.credential-secret-arn=${CREDENTIAL_SECRET_ARN} 